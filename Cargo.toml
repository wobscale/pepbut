[package]
name = "pepbut"
version = "0.1.0"
authors = ["Iliana Weller <ilianaw@buttslol.net>"]
license = "MIT/Apache-2.0"

[profile.release]
lto = true
panic = "abort"

[dependencies]
lazy_static = "1.0"
trust-dns = { git = "https://github.com/bluejekyll/trust-dns", default-features = false }
trust-dns-proto = { git = "https://github.com/bluejekyll/trust-dns", default-features = false }
trust-dns-server = { git = "https://github.com/bluejekyll/trust-dns", default-features = false }
